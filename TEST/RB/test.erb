<% # This is a comment %>
<!DOCTYPE html>
<html>
<head>
    <title>Test</title>
</head>
<body>
    <a href="/time">Time</a>
    <p>
        <%= if ( person_array.size \
                 > 0 )
                person_array.size.to_s() + " persons.";
            else
                "Nobody.";
            end %>
    </p>
    <table>
        <tr id="<%%Ignored%>IGNORED%>" class="<%=processed="PROCESSED"%>">
            <th>Name</th>
            <th>Age</th>
        </tr>
        <% person_array.each do | person | %>
            <tr>
                <td><%= HTML.escape( person.name ) %></td>
                <td><%= person.age %></td>
            </tr>
        <% end %>
    </table>
</body>
</html>
<% # This is a comment. %>
